# Tell Grafana to load dashboard JSON files from the mounted folder.
apiVersion: 1

providers:
  - name: Lab dashboards
    orgId: 1
    folder: Homelab
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards
